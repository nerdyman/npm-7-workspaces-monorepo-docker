{
  "name": "npm-monorepo-workspaces-docker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "docker": "docker build --no-cache --tag npm-workspaces .",
    "start-all": "npm run start --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm": "^7.18.1"
  }
}
